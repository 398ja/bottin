# Tomcat's RemoteIpValve applies X-Forwarded-For, but only when the immediate peer
# matches internal-proxies. That list is empty here, so by default nothing is
# trusted to state the caller's address and a direct caller cannot mint a fresh
# identity per request to walk past the rate limiter.
#
# Tomcat's own default trusts every private range, which is not safe for this
# deployment: with the port published from Docker the peer is the bridge gateway,
# a private address, so any caller reaching the port would be trusted.
#
# Behind a real proxy, set BOTTIN_TRUSTED_PROXIES to a regex matching it (e.g.
# "10\.0\.0\.5" or "172\.18\.\d{1,3}\.\d{1,3}"). Until you do, every client shares
# one rate-limit bucket keyed on the proxy's address.
server:
  forward-headers-strategy: native
  tomcat:
    remoteip:
      internal-proxies: ${BOTTIN_TRUSTED_PROXIES:}

spring:
  application:
    name: bottin

  # Database - use H2 by default for development
  datasource:
    url: jdbc:h2:mem:bottin;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  # JPA Configuration
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true

  # Flyway
  flyway:
    enabled: true
    locations: classpath:db/migration

  # H2 Console (development only)
  h2:
    console:
      enabled: true
      path: /h2-console

# Bottin Configuration
bottin:
  default-domain: ""

  # Admin credentials
  admin:
    username: admin
    # password: Set via BOTTIN_ADMIN_PASSWORD environment variable

  # Credential for machine callers (the client UI, scripts): API access, no admin
  # rights. Kept separate from the admin password so issuing it to a service does
  # not issue the admin credential.
  # api.password: Set via BOTTIN_API_PASSWORD environment variable

  # API documentation
  api:
    title: Bottin NIP-05 Registry API
    description: REST API for managing NIP-05 identity records and domains
    version: 1.0.0

  # Profile reach (follower count) calculation
  reach:
    enabled: true
    calculation-cron: "0 0 */6 * * ?"   # every 6 hours
    relay-timeout-seconds: 12
    max-followers-per-relay: 5000
    max-profiles-per-run: 10000
    default-relays:
      - wss://relay.damus.io
      - wss://nos.lol
      - wss://relay.nostr.band         # indexing relay for coverage

# OpenAPI / Swagger
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    operationsSorter: method
    tagsSorter: alpha

# Logging
logging:
  level:
    xyz.tcheeric.bottin: DEBUG
    org.springframework.security: INFO
